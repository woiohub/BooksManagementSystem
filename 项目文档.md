# 图书借阅管理系统 — 项目文档

## 一、项目概述

### 1.1 项目背景

本项目为数据库课程实践「图书借阅管理系统」的软件开发部分，严格遵循 Jinja 分支技术路线，采用 **Flask + Jinja2** 前后端不分离架构。系统通过 Python 高级语言实现数据库连接与操作，封装图书借阅核心业务逻辑，开发可动态交互的可视化界面，完整支撑图书馆馆藏管理、借阅管控、统计排行等业务场景。

### 1.2 核心目标

| 目标 | 说明 |
|------|------|
| 数据库连接与操作 | 通过 SQLAlchemy ORM 以 Python 语法替代原生 SQL，实现跨数据库适配 |
| 核心业务逻辑封装 | 借阅权限校验、超期违约金自动计算、库存同步、热度统计算法 |
| 可视化交互界面 | 基于 Jinja2 模板引擎实现动态页面渲染与表单交互 |

### 1.3 运行环境

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows / Linux / macOS |
| Python | 3.9+ |
| 数据库 | SQL Server（需提前执行 SQL Server数据库设计.txt 建库建表） |
| 浏览器 | Chrome / Edge / Firefox 等现代浏览器 |

---

## 二、技术栈选型

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 开发语言 | Python | 主开发语言，简洁高效 |
| Web 框架 | Flask 3.1 | 轻量级服务端框架，适配课程项目快速开发 |
| 数据库交互 | SQLAlchemy 2.0 | ORM 框架，以 Python 语法替代原生 SQL，屏蔽多数据库语法差异 |
| 前端渲染 | Jinja2 | 与 Flask 高度原生融合的模板引擎，无需前端框架基础 |
| 底层数据库 | SQL Server | 承载业务数据，系统不自动建表，连接已有数据库 |

---

## 三、系统架构

### 3.1 分层架构

采用经典三层架构，基于 Flask 生态实现一体化前后端逻辑：

```
┌─────────────────────────────────────────────────────┐
│                    表现层（Presentation）              │
│          Jinja2 模板 + Flask 蓝图路由                   │
│    页面渲染 · 用户交互 · 表单提交 · 动态数据展示         │
├─────────────────────────────────────────────────────┤
│                  业务逻辑层（Business）                 │
│          services/ 服务层                              │
│    借阅权限校验 · 违约金计算 · 热度统计 · 排行算法       │
├─────────────────────────────────────────────────────┤
│                  数据访问层（Data Access）              │
│          SQLAlchemy ORM 实体映射                       │
│    6 个实体类 · CRUD 封装 · 关联查询 · 事务管理         │
├─────────────────────────────────────────────────────┤
│                    数据层（Data）                       │
│          SQLite / SQL Server 数据库                    │
│    6 张业务表 · 索引 · 触发器 · 存储过程（SQL Server）   │
└─────────────────────────────────────────────────────┘
```

### 3.2 项目目录结构

```
BooksManagementSystem/
├── app.py                       # Flask 应用入口，工厂模式，数据库初始化
├── config.py                    # 配置文件（数据库连接、密钥等）
├── models.py                    # SQLAlchemy ORM 实体类定义（6 个模型）
├── requirements.txt             # Python 依赖清单
├── 项目文档.md                   # 本文档
├── SQL Server数据库设计.txt       # 数据库设计原始文档
├── routes/                      # 路由层（Flask Blueprint）
│   ├── __init__.py
│   ├── auth.py                  # 认证模块（用户登录/管理员登录/登出）
│   ├── users.py                 # 用户管理（CRUD）- 需管理员权限
│   ├── books.py                 # 图书管理（CRUD + 搜索）- 管理功能需管理员权限
│   ├── categories.py            # 分类管理（CRUD）- 需管理员权限
│   ├── borrow.py                # 借阅归还（借书/还书/记录/超期）- 需用户权限
│   └── stats.py                 # 统计排行（看板/图书排行/分类排行/用户排行）- 需管理员权限
├── services/                    # 业务逻辑层
│   ├── __init__.py
│   ├── borrow_service.py        # 借阅核心业务（权限校验、应还日期、违约金计算）
│   └── stats_service.py         # 统计业务（多维度热度排行）
├── utils/                       # 工具模块
│   ├── __init__.py
│   └── decorators.py            # 权限装饰器（admin_required, user_required）
├── templates/                   # Jinja2 模板（表现层）
│   ├── base.html                # 基础布局模板（导航栏、页脚、动态菜单）
│   ├── index.html               # 首页（快捷入口 + 管理面板）
│   ├── login.html               # 登录选择页（用户登录/管理员登录）
│   ├── user_login.html          # 用户登录页（仅需用户ID）
│   ├── admin_login.html         # 管理员登录页（用户名+密码）
│   ├── users/                   # 用户管理页面
│   │   ├── list.html
│   │   ├── add.html
│   │   └── edit.html
│   ├── books/                   # 图书管理页面
│   │   ├── list.html            # 图书列表（管理员）
│   │   ├── add.html             # 添加图书（管理员）
│   │   ├── edit.html            # 编辑图书（管理员）
│   │   └── search.html          # 图书搜索（用户）
│   ├── categories/              # 分类管理页面
│   │   ├── list.html
│   │   ├── add.html
│   │   └── edit.html
│   ├── borrow/                  # 借阅归还页面
│   │   ├── borrow.html          # 借书操作（自动获取当前用户）
│   │   ├── return.html          # 还书操作（显示个人借阅记录）
│   │   ├── records.html         # 借阅记录列表（统一字段和排序）
│   │   ├── overdue.html         # 超期未还列表
│   │   └── quick_search.html    # 快速查找
│   └── stats/                   # 统计排行页面
│       ├── dashboard.html       # 综合看板
│       ├── book_ranking.html    # 图书借阅热度排行
│       ├── category_ranking.html # 分类借阅频率排行
│       └── user_ranking.html    # 用户借阅频度排行
└── static/                      # 静态资源
    └── css/
        └── style.css            # 全局样式表（响应式布局）
```

---

## 四、数据库设计

### 4.1 E-R 模型

系统包含 5 张核心业务表，实体关系如下：

- **Category**（分类） 1:N **Book**（图书）
- **User**（用户） 1:N **BorrowRecord**（借阅记录）
- **Book**（图书） 1:N **BorrowRecord**（借阅记录）
- **Admin**（管理员）独立实体

> 注：用户类型已合并至 User 表，不再有独立的 UserType 实体。

### 4.2 表结构详情

#### Category（图书分类表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| cat_id | CHAR(10) | PK | 中图法分类号（如TP3） |
| cat_name | NVARCHAR(50) | NOT NULL | 分类名称 |
| location | NVARCHAR(100) | | 馆藏位置 |
| remark | NVARCHAR(200) | | 备注 |

#### Book（图书表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| book_id | INT | PK, AUTO_INCREMENT | 图书编号 |
| isbn | VARCHAR(20) | UNIQUE, NOT NULL | ISBN编号 |
| title | NVARCHAR(200) | NOT NULL | 书名 |
| author | NVARCHAR(100) | | 作者 |
| publisher | NVARCHAR(100) | | 出版社 |
| pub_year | INT | | 出版年份 |
| cat_id | CHAR(10) | FK → Category | 分类编号 |
| total_stock | INT | NOT NULL, CHECK ≥0 | 总库存 |
| available_stock | INT | NOT NULL, CHECK ≥0 | 可借库存 |
| shelf_location | NVARCHAR(50) | | 架位号 |

#### User（用户表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| user_id | INT | PK, AUTO_INCREMENT | 用户编号 |
| name | NVARCHAR(50) | NOT NULL | 姓名 |
| password | NVARCHAR(50) | NOT NULL | 登录密码 |
| user_type | NVARCHAR(20) | NOT NULL | 教师 / 研究生 / 本科生 |
| max_borrow | INT | NOT NULL | 最大可借数量 |
| days_limit | INT | NOT NULL | 借阅期限（天） |
| penalty_per_day | DECIMAL(10,2) | NOT NULL | 超期日违约金（元） |
| borrowed_count | INT | DEFAULT 0 | 当前已借数量 |
| penalty_total | DECIMAL(10,2) | DEFAULT 0 | 累计违约金 |

#### BorrowRecord（借阅记录表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| record_id | INT | PK, AUTO_INCREMENT | 记录编号 |
| book_id | INT | FK → Book | 图书编号 |
| user_id | INT | FK → User | 用户编号 |
| borrow_date | DATE | NOT NULL | 借阅日期 |
| due_date | DATE | NOT NULL | 应还日期 |
| return_date | DATE | NULL | 实际归还日期 |
| penalty | DECIMAL(10,2) | DEFAULT 0 | 违约金 |
| status | TINYINT | DEFAULT 0 | 0=借出中, 1=已归还 |

#### Admin（管理员表）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| admin_id | INT | PK, AUTO_INCREMENT | 管理员编号 |
| username | VARCHAR(30) | UNIQUE, NOT NULL | 用户名 |
| password | VARCHAR(50) | NOT NULL | 密码 |

### 4.3 预置数据

#### 用户类型配额

| 类型 | 可借数 | 期限 | 日违约金 |
|------|--------|------|----------|
| 教师 | 20本 | 60天 | 0.50元 |
| 研究生 | 15本 | 45天 | 0.30元 |
| 本科生 | 10本 | 30天 | 0.20元 |

#### 图书分类

| 编号 | 名称 | 馆藏位置 |
|------|------|----------|
| TP3 | 计算机技术 | A区3排 |
| H31 | 英语 | B区2排 |
| O13 | 数学 | C区1排 |
| I247 | 小说 | D区4排 |
| K2 | 历史地理 | E区5排 |

#### 预置图书（15本）

涵盖数据库系统概念、操作系统导论、计算机网络、算法导论、新概念英语、高等数学、三体、活着、机器学习等经典书目。

#### 预置用户（7名）

| 姓名 | 密码 | 类型 |
|------|------|------|
| 张教师 | 123456 | 教师 |
| 李教授 | 123456 | 教师 |
| 王研究生 | 123456 | 研究生 |
| 赵研究生 | 123456 | 研究生 |
| 陈本科生 | 123456 | 本科生 |
| 刘本科生 | 123456 | 本科生 |
| 孙本科生 | 123456 | 本科生 |

#### 默认管理员

| 用户名 | 密码 |
|--------|------|
| admin | admin123 |

---

## 五、功能模块

### 5.1 认证模块（登录/登出）

系统提供双入口登录机制，区分普通用户和管理员两种身份。

| 功能 | 路由 | 说明 |
|------|------|------|
| 登录选择 | GET `/login` | 登录方式选择页面 |
| 用户登录 | GET/POST `/user_login` | 输入用户ID和密码 |
| 管理员登录 | GET/POST `/admin_login` | 输入用户名和密码 |
| 用户退出 | GET `/logout_user` | 清除用户会话，返回首页 |
| 管理员退出 | GET `/logout_admin` | 清除管理员会话，返回首页 |
| 全部登出 | GET `/logout` | 清除所有会话（兼容旧版） |

**权限控制机制**：

- **用户权限**：使用 `@user_required` 装饰器保护路由，未登录用户访问时重定向到用户登录页
- **管理员权限**：使用 `@admin_required` 装饰器保护路由，未登录管理员访问时重定向到管理员登录页
- **导航栏动态显示**：根据登录身份动态显示不同菜单项
  - 用户登录后：显示「借阅图书」「归还图书」「借阅记录」「图书搜索」
  - 管理员登录后：显示「管理」下拉菜单（用户管理、图书管理、分类管理）和「统计」下拉菜单
  - 未登录：仅显示「登录」入口
- **独立退出机制**：支持用户和管理员独立退出，多身份登录时可分别退出

### 5.2 用户管理模块（管理员专属）

维护教师、研究生、本科生三类用户信息，绑定差异化借阅权限。

| 功能 | 路由 | 权限 | 说明 |
|------|------|------|------|
| 用户列表 | GET `/users/` | 管理员 | 支持按姓名搜索，展示用户类型、已借数、违约金 |
| 添加用户 | GET/POST `/users/add` | 管理员 | 选择用户类型，系统自动关联权限 |
| 编辑用户 | GET/POST `/users/edit/<id>` | 管理员 | 修改姓名、密码、用户类型 |
| 删除用户 | POST `/users/delete/<id>` | 管理员 | 校验是否有未归还图书，防止误删 |

### 5.3 图书与分类管理模块

管理图书基础信息、中图分类、馆藏位置与实时库存。

| 功能 | 路由 | 权限 | 说明 |
|------|------|------|------|
| 图书列表 | GET `/books/` | 管理员 | 支持按书名/作者搜索、按分类筛选 |
| 添加图书 | GET/POST `/books/add` | 管理员 | 录入 ISBN、书名、分类、库存等 |
| 编辑图书 | GET/POST `/books/edit/<id>` | 管理员 | 库存修改时校验不低於借出数量 |
| 删除图书 | POST `/books/delete/<id>` | 管理员 | 校验是否有未归还记录 |
| 图书搜索 | GET `/books/search` | 用户 | 多维度组合搜索，展示图书详细信息 |
| 分类列表 | GET `/categories/` | 管理员 | 展示所有分类及馆藏位置 |
| 添加分类 | GET/POST `/categories/add` | 管理员 | 新增中图分类 |
| 编辑分类 | GET/POST `/categories/edit/<id>` | 管理员 | 修改分类信息 |
| 删除分类 | POST `/categories/delete/<id>` | 管理员 | 校验分类下是否有图书 |

**图书搜索功能**：

支持多维度组合搜索，搜索字段包括：
- 图书ID（精确匹配）
- ISBN（模糊匹配）
- 书名（模糊匹配）
- 作者（模糊匹配）
- 出版社（模糊匹配）
- 分类（下拉选择，精确匹配）

搜索结果展示：图书ID、ISBN、书名、作者、出版社、分类名称、总库存、可借数量、架位号。

### 5.4 借阅归还核心模块

实现借书、还书全流程操作，是系统核心业务单元。

| 功能 | 路由 | 权限 | 说明 |
|------|------|------|------|
| 借阅图书 | GET/POST `/borrow/` | 用户 | 自动获取当前登录用户ID，输入图书ID即可借阅 |
| 归还图书 | GET/POST `/borrow/return_page` | 用户 | 输入记录ID，自动计算超期违约金，显示个人借阅记录 |
| 借阅记录 | GET `/borrow/records` | 用户 | 按用户ID/状态筛选，展示全部记录，按记录ID升序排列 |
| 超期未还 | GET `/borrow/overdue` | 用户 | 列出所有超期未归还的图书 |
| 快速查找 | GET `/borrow/quick_search` | 用户 | 按书名/作者/ISBN/姓名多条件检索 |

**借阅流程改进**：

1. 用户登录后，系统自动从 session 获取 `user_id`，无需手动输入
2. 借阅页面显示当前登录用户信息和可借数量
3. 提供「查看/搜索图书」按钮，可跳转到图书搜索页面
4. 借阅成功后显示绿色提示：「借阅成功！应还日期：YYYY-MM-DD」
5. 超量借阅时显示红色提示：「借阅失败：您已达到最大可借数量（X本），请先归还后再借」

**归还界面增强**：

1. 归还页面下方显示当前用户的个人借阅记录列表
2. 记录字段：记录ID、图书名称、图书ID、借阅日期、应还日期、归还日期、违约金、状态
3. 归还成功后自动刷新列表
4. 归还成功提示：「归还成功！」或「归还成功！超期X天，违约金Y元」

**借阅记录优化**：

1. 所有借阅记录列表统一按 `record_id` **升序**排列
2. 统一字段展示：记录ID、图书名称、图书ID、用户名称、用户ID、借阅日期、应还日期、归还日期、违约金(元)、状态
3. 状态标签：借出中（红色）、已归还（绿色）

**借阅业务规则**：

1. 校验用户是否存在
2. 校验图书是否存在且库存充足（`available_stock > 0`）
3. 校验用户是否有剩余借阅额度（`borrowed_count < max_borrow`）
4. 自动计算应还日期：`borrow_date + days_limit`
5. 创建借阅记录（status=0），同步更新图书库存和用户已借数

**归还业务规则**：

1. 校验记录是否存在且未归还（status=0）
2. 设置归还日期和状态（status=1）
3. 恢复图书库存（`available_stock + 1`）
4. 减少用户已借数（`borrowed_count - 1`）
5. 若超期：`违约金 = (归还日 - 应还日) × 日违约金标准`，累加到用户总违约金

### 5.5 统计排行模块（管理员专属）

统计单本图书借阅热度、分类借阅频率、用户借阅频度，生成多维度排行榜。

| 功能 | 路由 | 权限 | 说明 |
|------|------|------|------|
| 综合看板 | GET `/stats/` | 管理员 | 馆藏/用户/借出/超期数量、TOP10 排行 |
| 图书排行 | GET `/stats/books` | 管理员 | 图书借阅热度排行，支持 TOP 5/10/20 |
| 分类排行 | GET `/stats/categories` | 管理员 | 分类借阅频率排行 |
| 用户排行 | GET `/stats/users` | 管理员 | 用户借阅频度排行，支持 TOP 5/10/20 |

---

## 六、安装部署

### 6.1 环境准备

确保已安装 Python 3.9 或更高版本：

```bash
python --version
```

### 6.2 安装依赖

```bash
cd BooksManagementSystem
pip install -r requirements.txt
```

依赖清单：

```
Flask==3.1.0
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.36
pyodbc==5.2.0
```

### 6.3 准备数据库

系统**不会自动建表**，需提前在 SQL Server 中执行 `SQL Server数据库设计.txt` 中的建库建表脚本，并插入基础数据。

1. 打开 SQL Server Management Studio (SSMS)
2. 执行 `SQL Server数据库设计.txt` 全部内容
3. 确认数据库 `LibraryManagement` 已创建，6 张表已生成

### 6.4 配置数据库连接

编辑 `config.py`，修改连接字符串以匹配你的 SQL Server 环境：

```python
# Windows 身份验证（默认）
SQLALCHEMY_DATABASE_URI = (
    'mssql+pyodbc://@localhost/LibraryManagementSystem?'
    'driver=ODBC+Driver+17+for+SQL+Server&'
    'trusted_connection=yes&'
    'TrustServerCertificate=yes'
)

# 用户名密码登录
SQLALCHEMY_DATABASE_URI = (
    'mssql+pyodbc://sa:your_password@localhost/LibraryManagementSystem?'
    'driver=ODBC+Driver+17+for+SQL+Server&'
    'TrustServerCertificate=yes'
)

# MySQL 连接
SQLALCHEMY_DATABASE_URI = (
    'mysql+pymysql://root:your_password@localhost/LibraryManagementSystem?charset=utf8mb4'
)
```

### 6.5 启动应用

```bash
python app.py
```

启动后访问 [http://127.0.0.1:5000](http://127.0.0.1:5000)。

---

## 七、使用说明

### 7.1 登录系统

系统提供两种登录方式：

**用户登录**：
1. 访问首页，点击导航栏「登录」
2. 选择「用户登录」
3. 输入用户ID（如 1 = 张教师）
4. 登录成功后，导航栏显示「借阅图书」「归还图书」「借阅记录」「图书搜索」等用户功能

**管理员登录**：
1. 访问首页，点击导航栏「登录」
2. 选择「管理员登录」
3. 输入用户名（admin）和密码（admin123）
4. 登录成功后，导航栏显示「管理」和「统计」下拉菜单

### 7.2 借阅图书（用户功能）

1. 用户登录后，点击导航栏「借阅图书」
2. 页面自动显示当前登录用户信息和可借数量
3. 可点击「查看/搜索图书」按钮搜索图书
4. 输入图书ID（如 1 = 数据库系统概念）
5. 点击「确认借阅」
6. 系统自动校验权限并显示应还日期
7. 借阅成功显示绿色提示：「借阅成功！应还日期：YYYY-MM-DD」

### 7.3 归还图书（用户功能）

1. 用户登录后，点击导航栏「归还图书」
2. 输入借阅记录 ID（可在下方「我的借阅记录」中查看）
3. 点击「确认归还」
4. 系统自动计算超期天数及违约金（如有）
5. 归还成功显示提示：「归还成功！」或「归还成功！超期X天，违约金Y元」
6. 页面自动刷新个人借阅记录列表

### 7.4 图书搜索（用户功能）

1. 用户登录后，点击导航栏「图书搜索」
2. 可输入多个搜索条件：图书ID、ISBN、书名、作者、出版社、分类
3. 点击「搜索」按钮
4. 结果展示：图书ID、ISBN、书名、作者、出版社、分类名称、总库存、可借数量、架位号

### 7.5 查看借阅记录（用户功能）

1. 用户登录后，点击导航栏「借阅记录」
2. 可按用户ID或状态筛选
3. 记录按记录ID升序排列
4. 展示字段：记录ID、图书名称、图书ID、用户名称、用户ID、借阅日期、应还日期、归还日期、违约金(元)、状态

### 7.6 管理数据（管理员功能）

管理员登录后：

- **用户管理**：增删改查用户，修改用户类型将自动调整借阅权限
- **图书管理**：增删改查图书，按分类筛选，库存管理
- **分类管理**：维护中图分类与馆藏位置

### 7.7 查看统计（管理员功能）

管理员登录后：

1. 导航栏「统计」→「综合看板」查看全局数据概览
2. 「图书排行」查看最热门图书
3. 「分类排行」查看最受欢迎分类
4. 「用户排行」查看借阅最活跃用户

---

## 八、业务规则速查

### 借阅权限矩阵

| 用户类型 | 最大可借 | 借阅期限 | 日违约金 |
|----------|----------|----------|----------|
| 教师 | 20 本 | 60 天 | 0.50 元 |
| 研究生 | 15 本 | 45 天 | 0.30 元 |
| 本科生 | 10 本 | 30 天 | 0.20 元 |

### 违约金计算公式

```
违约金 = (实际归还日期 - 应还日期) × 日违约金标准
```

- 按期或提前归还：违约金 = 0
- 超期归还：按天累计，自动更新借阅记录和用户总违约金

### 库存同步规则

| 操作 | 图书 available_stock | 用户 borrowed_count |
|------|---------------------|---------------------|
| 借书 | -1 | +1 |
| 还书 | +1 | -1 |

---

## 九、数据库连接原理

### 9.1 架构说明

系统**不负责建表**，只负责**连接已有数据库并操作数据**：

```
┌──────────────┐     SQLAlchemy ORM      ┌──────────────────┐
│  Flask 应用   │ ◄──────────────────────► │  SQL Server 数据库 │
│  (app.py)    │   pyodbc 驱动连接         │  (已有库表+数据)   │
└──────────────┘                          └──────────────────┘
```

- **app.py**：Flask 应用入口，通过 `db.init_app(app)` 将 SQLAlchemy 绑定到 Flask
- **config.py**：配置数据库连接 URI（`mssql+pyodbc://...`），指向已有 SQL Server 实例
- **models.py**：ORM 实体类映射已有表结构，**不执行 DDL**（无 `db.create_all()`）
- **routes/**：路由层接收请求，调用 services 层，通过 ORM 操作数据库
- **services/**：业务逻辑层，封装借阅校验、违约金计算等核心规则

### 9.2 连接流程

1. 启动时 `db.init_app(app)` 读取 `config.py` 中的连接 URI
2. SQLAlchemy 通过 `pyodbc` 驱动连接 SQL Server
3. 请求到达时，Flask 创建请求上下文，SQLAlchemy 提供 `db.session` 数据库会话
4. 路由通过 `db.session.query()` / `db.session.add()` / `db.session.commit()` 操作数据
5. 请求结束时自动关闭会话，连接归还连接池

### 9.3 管理员登录逻辑

管理员登录认证的是**数据库中 Admin 表**的数据，而非本地配置：

```
用户输入 admin / admin123
       │
       ▼
auth.py: Admin.query.filter_by(username='admin', password='admin123').first()
       │
       ▼
SQLAlchemy 生成: SELECT * FROM Admin WHERE username='admin' AND password='admin123'
       │
       ▼
SQL Server 执行查询，返回匹配的管理员记录
       │
       ▼
匹配成功 → 写入 session → 登录成功
```

---

## 十、设计亮点

1. **三层架构清晰分离**：表现层（路由+模板）、业务逻辑层（services）、数据访问层（ORM），职责明确，易于维护和扩展。

2. **SQLAlchemy ORM 抽象**：完全通过 Python 语法操作数据库，修改 `config.py` 一行配置即可切换 SQL Server / MySQL，无需修改任何业务代码。

3. **借阅权限自动校验**：借书时自动根据用户类型检查剩余可借额度，防止超限借阅；还书时自动计算超期违约金，保证业务规则统一落地。

4. **多维度统计分析**：从图书热度、分类频率、用户活跃度三个维度生成排行榜，支持 TOP N 动态切换，支撑管理员运营分析。

5. **响应式界面设计**：基于 CSS Grid/Flexbox 的响应式布局，适配桌面端与移动端，导航栏含下拉菜单，交互流畅。

6. **数据完整性保护**：删除用户时校验是否有未归还图书，删除图书时校验是否有借出记录，删除分类时校验是否有关联图书，防止脏数据产生。

---

## 十一、安全性与健壮性

### 11.1 输入验证

系统对所有用户输入进行严格验证：

- **ID 参数验证**：所有 ID 参数必须为正整数，防止非法输入导致系统崩溃
- **必填字段验证**：表单提交时验证必填字段，防止空值提交
- **业务规则验证**：借阅时验证库存和配额，归还时验证记录归属

### 11.2 权限控制

- **用户权限隔离**：普通用户只能查看和操作自己的借阅记录，不能访问管理功能
- **管理员权限保护**：管理功能需要管理员登录，使用 `@admin_required` 装饰器保护
- **越权操作防护**：归还图书时验证记录归属，防止用户归还他人借阅记录

### 11.3 异常处理

- **数据库事务管理**：所有数据库操作使用 try-except 包裹，失败时自动回滚
- **友好错误提示**：异常发生时返回用户友好的错误信息，不暴露技术细节
- **日志记录**：使用 logger 记录异常堆栈，便于问题排查

### 11.4 性能优化

- **数据库索引**：为高频查询字段添加索引（isbn、title、author、book_id、user_id、status）
- **查询优化**：使用 JOIN 查询减少数据库访问次数，避免 N+1 问题
- **分页支持**：大数据量列表支持分页，提升页面加载速度

### 11.5 数据库触发器集成

系统使用数据库触发器自动维护数据一致性：

- **库存同步触发器**：借阅时自动减少可借库存，归还时自动增加
- **借阅计数触发器**：借阅时自动增加已借数量，归还时自动减少
- **违约金计算触发器**：归还时自动计算超期违约金并更新记录

应用层代码不直接修改这些字段，完全依赖触发器保证数据一致性，避免双重更新导致的错误。